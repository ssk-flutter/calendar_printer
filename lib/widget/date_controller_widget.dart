import 'package:flutter/material.dart';

class DateControllerWidget extends StatelessWidget {
  const DateControllerWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('date controller here');
  }
}
